{% extends 'ProjectsBlogBundle::layout.html.twig' %}
{% block content %}
    <section class="posts">
    {% autoescape false %}
        <article>
            <header>
                <h2>{{page.title}}</h2>
            </header>
            <p>{{page.post}}</p>
        </article>
    {% endautoescape %}
    </section> <!-- .posts -->
{% endblock %}
